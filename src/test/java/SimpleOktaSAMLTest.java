import org.junit.Test;
import org.world.okta.saml.SimpleOktaSAMLApplication;

import static org.junit.Assert.*;

public class SimpleOktaSAMLTest {
    @Test public void testSomeLibraryMethod() {
    	SimpleOktaSAMLApplication classUnderTest = new SimpleOktaSAMLApplication();
        assertTrue("someLibraryMethod should return 'true'", classUnderTest.sayYesMethod());
    }
}
